import styled from 'styled-components';

export const AuthWrapper = styled.div`
  width: 100vw;
  height: 100vh;
  background-color: ${({ theme }) => theme.colors.purple800};
  background-image: linear-gradient(315deg, #2a2a72 30%, #009ffd 100%);
`;
